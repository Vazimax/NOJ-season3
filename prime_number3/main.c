#include <stdio.h>

int main()
{

    /*
	int a = 500;
	int  b = 800;
	int i, j, flag;
	int sum = 0;
    int count = 0;

	for (i = a; i <= b; i++) {
		if (i == 1 || i == 0)
			continue;
		flag = 1;

		for (j = 2; j <= i / 2; ++j) {
			if (i % j == 0) {
				flag = 0;
				if (i%2 == 0){
                    sum -= i;
                else{
                    sum += i;
                }
				}
				break;
			}
		}

		if (flag == 1)
			count += 1;

	}

	printf("%d %d",count,sum);
	*/

	return 0;
}
